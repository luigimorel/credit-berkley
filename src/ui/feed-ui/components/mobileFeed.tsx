import { Box } from '@material-ui/core';
import MobileMenu from './mobileMenu';

const MobileFeed = () => {
    return (
        <Box display="flex">
            <MobileMenu />
        </Box>
    );
};

export default MobileFeed;
